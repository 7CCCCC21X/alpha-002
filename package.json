{
  "name": "bsc-initpool-tg-bot",
  "version": "1.0.0",
  "description": "BSC on-chain initializePool / addPoolOwners listener that pushes alerts to Telegram",
  "type": "module",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "vitest": "^2.1.4"
  }
}
